# Molecule managed
---
command:
  help:
    exit-status: 0
    exec: "systemctl is-active openshift-cam"
    stdout:
      - "/active/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
